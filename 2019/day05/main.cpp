#include <fstream>
#include <iostream>
#include <string>

#include "functions.h"


int main() {
    std::ifstream file("input.txt");
    std::string line{};
    while (std::getline(file, line)) {

    }
    
    
    return 0;
}
